SELECT person_name, COUNT(*) FROM results 
WHERE pos = 1 AND round_type_id in ('C', 'F') AND Best > 0
AND competition_id LIKE 'WC2%' 
GROUP BY person_id
ORDER BY COUNT(*) DESC

-- simple query for returning who has the most event wins across all World Championships 
